{
  "name": "frontend",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn install && cd packages/common && yarn run build && cd ../homescreen && yarn build && cd ../quiz-creator && yarn build && cd ../authentication && yarn build && cd ../presentation && yarn build",
    "dev-server": "yarn run build && cd ./dev-server && npm start"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.3"
  }
}